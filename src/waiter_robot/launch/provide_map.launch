<launch>

  <arg name="map_file" default="$(find waiter_robot)/map/cafe2d.yaml"/>

  <!-- Run the map server -->
  <node name="map_server" pkg="map_server" type="map_server" args="$(arg map_file)" />

</launch>
